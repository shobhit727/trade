# NOTE: target-cpu=native breaks cached builds across heterogeneous runners
# (proc-macro .so artifacts built for AVX512 crash with SIGILL on older CPUs).
# For local builds, opt in via env: CARGO_RUSTFLAGS="-C target-cpu=native"
