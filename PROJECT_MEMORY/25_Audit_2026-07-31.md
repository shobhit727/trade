# Audit 2026-07-31

> **Confidence**: High (compiled + grep-verified).
> **Scope**: `plan.md` vs `PROJECT_MEMORY/` vs actual repo state.
> **Method**: full `src/` + `tests/` + compose + k8s + crates + configs file walk. `python3 -m py_compile` clean on `src/`. `pytest` not run (no network → no deps).

## TL;DR

`plan.md` and `PROJECT_MEMORY/` are **stale** in opposite directions. `plan.md` over-claims (e.g. `[x] ML-driven strategy` while `strategies/ml_strategy.py` does not exist). `PROJECT_MEMORY/` under-claims (e.g. `data/features.py 🔲 Missing`, `pyproject.toml 🔲 Missing`, `execution/venue/binance.py 🔲 Missing` — all wrong, they exist or are implemented).

The repo is **far more developed** than `00_Project_Overview.md` and `12_Feature_Status.md` reflect. The Memory "Last Updated 2026-07-29" snapshots are pre-binance-venue, pre-backtest-runner, pre-validation-real-math, pre-ml, pre-router, pre-adverse-selection.

Concrete deltas below. Anything not listed = agreement between docs and code.

---

## Discrepancies: docs say, code says

### `plan.md` over-claims (false ✅)

| plan.md claim | Reality |
|---|---|
| Phase 3: `[x] Position management primitives in BaseStrategy` | BaseStrategy exposes lifecycle only; no scaling/stop helpers. |
| Phase 4: `[x] ML-driven strategy (strategies/ml_strategy.py)` | `src/cryptobot/strategies/ml_strategy.py` does not exist. |
| Phase 5: `[x] Pre-trade risk checks (exposure, drawdown via kill switch, notional bounds)` | `manager.py:43` notional check still does `notional_price = price or order.price or order.avg_fill_price` — bypassed (B038) for unfilled market orders rather than properly bounded. |
| Phase 7: `[x] BinanceVenue` "via ccxt.async_support" | `binance.py:240` lines, present and tested (`test_binance_venue.py`). OK, but `plan.md` section 2 still says `binance.py 🔲 Missing` — direct contradiction in same file. |
| Section 2 table: `execution/venue/binance.py` `🔲 Missing` | File exists, 240 lines, test exists. |
| Section 11: many `🔲` for files that now exist | `ml/features.py`, `ml/online.py`, `ml/models/direction.py`, `backtest/runner.py`, `backtest/data.py`, `backtest/reporting.py`, `utils/health_server.py` all exist. |
| Phase 8: `[x] Real walk-forward / Monte Carlo in backtest/validation.py` | Verified — `validation.py` 148 lines with real math. OK. But section 2 table still says `⚠️ Walk-forward + Monte Carlo stubs; needs real math` — stale. |
| Phase 6: `[x] Direction classifier (sklearn logreg preferred, pure-numpy fallback)` | `ml/models/direction.py` does this. OK. |
| Section 5b: Rust crates "Currently: `Cargo.toml` only, no crate members" | Manifest-only is wrong. All 7 crate dirs exist with `Cargo.toml` + `benches/` + `src/` + `tests/`. But 6/7 are empty scaffolding — no `lib.rs`. `cryptobot-core/src/{events,math,time,types}/` all empty. `cargo build` will fail. |
| Section 8: `deploy/k8s/` claims Service + HPA + kustomization overlay | Only `00-namespace`, `01-config`, `02-secret`, `03-pvc`, `04-deployment`, `kustomization.yaml` — **no Service, no HPA**. |

### `PROJECT_MEMORY/` under-claims (false 🔲)

| File | Memory says | Reality |
|---|---|---|
| `00_Project_Overview.md` | `core/`: 5 files "implemented" | OK (5 exist). But missing `market_data/`, `data/`, `backtest/`, `utils/`, `strategies/`, `risk/`, `execution/`, `cli/`, `monitoring/` from "current state" summary. |
| `00_Project_Overview.md` | `ml/`: empty | `ml/{features.py, online.py, models/direction.py}` exist. |
| `00_Project_Overview.md` | `execution/venue/binance.py`: missing | 240-line file with `BinanceVenue` class. |
| `00_Project_Overview.md` | `tests/`: only `test_core_foundation.py` | 22 unit test files. |
| `00_Project_Overview.md` | `cargo workspace`: manifest only | Workspace exists; 6/7 crates scaffolded but empty. |
| `12_Feature_Status.md` | `execution/venue/binance.py` 🔲 | ✅ implemented (same file is contradictory with itself in section "Fixed this session 2026-07-31" lines). |
| `12_Feature_Status.md` | `pyproject.toml / setup.py` 🔲 | `pyproject.toml` 39 lines, installable. |
| `12_Feature_Status.md` | `migrations/*.sql` 🔲 | `001_extension.sql`, `002_hypertables.sql` present. |
| `12_Feature_Status.md` | `data/features.py` 🔲 | Confirmed missing. (Memory right here, `plan.md` wrong elsewhere.) |
| `12_Feature_Status.md` | `crates/cryptobot-*/` 🔲 | Workspace exists; manifest+empty scaffolding per crate. |
| `12_Feature_Status.md` | "Confidence: High" + "31 unit tests pass" | Can't verify test count (no pytest in env). 22 test files present. |

### `PROJECT_MEMORY/13_Bug_Tracker.md` stale

| Bug | Memory says | Reality |
|---|---|---|
| B025 | `Counter` for realized PnL — crash on negative | `monitoring/metrics.py:90` `total_pnl` is now `Gauge`. Resolved but still listed as Open. |
| B027 | `retry` jitter negative sleep | `utils/decorators.py:48,72` `sleep_time = max(0.0, sleep_time)`. Resolved but still listed Open. |
| B028 | `circuit_breaker` sync `run_until_complete` | `utils/decorators.py:228-237` raises `RuntimeError` in running loop. Resolved but still listed Open. |
| B037 | `total_pnl` `Counter` cannot decrement | Same as B025; resolved. |
| B039 | `clean_trades` allows non-numeric silently | `data/cleaning.py` was patched 2026-07-29 (per `12_Feature_Status.md`). Resolved but listed Open in one place, Resolved in another (duplicate row in tracker). |
| B049 | `StrategyRegistry.__new__` prints on import | `grep` for `print` in `strategies/base.py` — no output. Resolved but still listed Open. |
| B046 | `print(...)` throughout `backtest/engine.py` | `grep` for `print(` in `backtest/engine.py` — no output. Resolved but still listed Open. |

### `PROJECT_MEMORY/14_Technical_Debt.md` stale

- "no CI, no ruff, no mypy" — `ci.yml` has ruff + pyflakes. ruff installed via `lint` job. Resolved.
- "No `pyproject.toml` / `setup.py`" — `pyproject.toml` 39 lines. Resolved.
- "Critical debt: Replace `Counter` with `Gauge` for realized PnL — 1 hour High" — already done. Remove.
- "Bound `retry` jitter so sleep_time ≥ 0" — already done. Remove.
- "Fix `circuit_breaker` sync wrapper" — already done. Remove.
- "Remove `print(...)` from `core/clock.py`, `backtest/engine.py`, `strategies/base.py`" — done. Remove.

---

## Real bugs (newly observed)

### High

1. **`docker-compose.yml` references missing dirs** — `compose` will fail validation:
   - `monitoring/loki/local-config.yaml` (line 180)
   - `monitoring/promtail/config.yml` (line 206)
   - `monitoring/nginx/nginx.conf` + `monitoring/nginx/conf.d` (lines 383–384)
   - Neither dir exists. **The default profile cannot start.** Plan + memory claim compose "validates" — but only the `test` profile was tested.
   - Fix: either add the missing config stubs or remove the services from default profile.
   - **FIXED (2026-07-31)**: Scaffolded 3 missing dirs with minimal configs; `docker compose config` now passes.

2. **`requirements/prod.txt` lists `lightgbm>=4.5`** but `ml/models/direction.py` falls back to `sklearn`/`numpy` only — no `lightgbm` import. Either drop the dep or use it. Heavy native build; remove if unused.

3. **`configs/base.yaml` `ml.models` block** has `direction: type: lightgbm`, `volatility`, `regime: type: hmm` — but `ml/models/` only has `direction.py`. `ml/features.py` ignores `ml.models.*` config (it reads `MLSettings.features` flat list, not nested). Yaml has features that don't map to any field. Silent loss.

4. **`data/features.py` missing** — `ml/features.py` is the canonical one. `plan.md` section 2 lists it as `🔲 Missing`; section 11 lists it as `🔲` to create next; `12_Feature_Status.md` says `data/features.py 🔲`. Consistent. Just not done. This is a real gap, not a doc bug.

5. **Rust workspace does not build** — `Cargo.toml` lists 7 members; 6/7 are scaffolded with no `lib.rs`. `cargo build` will fail. `ci.yml` doesn't run `cargo`. Plan claims Rust layer is "pending" — actually is **non-functional scaffolding**.

### Medium

6. **Phase 4 says `ml_strategy.py` is done** — it is not. `plan.md` lies here.

7. **Strategy `enabled` list in YAML** lists `mean_reversion`, `trend_following`, `funding_arbitrage`, `statistical_arbitrage` — but registry wiring is by class name. `registry.py` is 3 lines (re-export only). No auto-load of strategies from config. Strategies will not be instantiated from `configs/base.yaml` `strategies.enabled` unless code reads it. No code reads it.

8. **Test count claim** — `12_Feature_Status.md` says "31 unit tests pass". 22 test files, each 5–15 tests, so plausible. But couldn't verify (no pytest). Confidence claim "High" is overstated until pytest actually runs.

9. **K8s manifests incomplete** — `deploy/k8s/` lacks `Service` and `HPA` that `plan.md` Phase 8 claims. Cluster-internal only.

10. **Hardcoded `MeanReversionStrategyPlaceholder` "65000" trigger** — listed in `00_Project_Overview.md` as fact; verified absent (placeholder removed in current `base.py`). `00_Project_Overview.md` stale.

11. **Smart router adversarial-test gap** — `test_smart_order_router.py` and `test_adverse_selection.py` exist, but no `test_risk_manager_integration.py` (real order path). Risk gate never exercised in test suite with full Engine→Risk→Venue flow.

12. **`monitoring/__init__.py` eagerly imports `metrics`** — `13_Bug_Tracker.md` B051 calls this out. Confirmed: any `import cryptobot.monitoring` requires `prometheus_client`. Tests that don't need metrics pay the tax.

### Low

13. **`dashes` cruft in plan.md** — section 3 directory tree has stray characters (`), )` after files). Cosmetic.

14. **Dead dirs** — `src/cryptobot/{allocator,altdata,api,exchanges,funding,xmr}/` all empty. Plan + memory don't mention them. Either delete or document.

15. **CI matrix** — `docker-buildx` only runs on `push` to main, never on PR. Multi-arch PRs unverified.

16. **`pytest.ini` and `pyproject.toml` duplicate config** — `pyproject.toml [tool.pytest.ini_options]` and `pytest.ini` both define `asyncio_mode = auto`. Mild drift risk.

---

## Plan ↔ Memory ↔ Code: alignment matrix

| Topic | plan.md | PROJECT_MEMORY | code |
|---|---|---|---|
| Event bus | ✅ | ✅ | ✅ (verified) |
| Clock | ✅ | ✅ | ✅ |
| Portfolio | ✅ | ✅ | ✅ |
| ML features | ✅ done | ✅ done (`ml/features.py`) | ✅ |
| ML models (direction) | ✅ | ✅ | ✅ |
| Walk-forward validation | ✅ (Phase 2 ✅) but section 2 ⚠️ | ✅ resolved 2026-07-29 | ✅ real math |
| Binance venue | ✅ Phase 7 / 🔲 section 2 | ✅ resolved 2026-07-29 (conflicts) | ✅ 240 lines |
| `data/features.py` | not mentioned | 🔲 | 🔲 missing (no file) |
| `ml_strategy.py` | ✅ done | not mentioned | 🔲 missing |
| K8s Service/HPA | ✅ | ✅ | 🔲 missing |
| `pyproject.toml` | not mentioned | 🔲 | ✅ exists |
| Migrations SQL | not mentioned | 🔲 | ✅ exists (2 files) |
| Rust crates | "manifest only" | 🔲 | manifest + empty dirs |
| Docker compose default profile | ✅ verified | ✅ verified | 🔲 missing monitoring/{loki,promtail,nginx} |
| Risk manager notional on no-price | (not mentioned) | ✅ resolved B038 | ✅ guarded (`notional_price is not None`) |
| Total PnL `Counter`→`Gauge` | not mentioned | B025 Open | ✅ Gauge |
| Retry jitter negative | not mentioned | B027 Open | ✅ clamped to 0 |
| Circuit breaker sync loop | not mentioned | B028 Open | ✅ raises RuntimeError |
| `print()` in clock/engine/strategies | not mentioned | B046/B049 Open | ✅ gone |

**Inconsistency count**: 26 distinct mismatches across the three sources.

---

## Confidence / Verification

- **High**: directory walk, file presence, line counts, key string matches (Counter/Gauge, print, sleep_time=max, ORDER_REJECTED emit, _flatten_yaml, etc.). All Python in `src/` and `tests/` parses with `ast`.
- **Medium**: behavior of risk/SOR/ML — not exercised end-to-end here.
- **Low**: live Binance behavior, k8s deployment validity, Docker compose runtime (no daemon).

**Skipped due to env limits** (no network, no pip, no Docker daemon): `pytest`, `ruff check`, `pyflakes`, `docker compose config`, `cargo check`, `maturin develop`.

---

## Top 5 fixes ordered by blast radius

1. **Docker compose** — remove or scaffold the three missing `monitoring/{loki,promtail,nginx}` dirs. Default profile is currently broken. → 30 min.
2. **Delete dead `src/cryptobot/{allocator,altdata,api,exchanges,funding,xmr}/` dirs** (or document why empty). → 5 min.
3. **Remove `lightgbm` from `requirements/prod.txt`** if unused. Drop ~hundreds of MB from Docker image and minutes from CI. → 5 min.
4. **Sync `PROJECT_MEMORY/` with current state**: close B025/B027/B028/B037/B046/B049; mark `binance.py` ✅; mark `pyproject.toml` ✅; mark `data/features.py` as the canonical one (rename reference); add 22nd test file. → 30 min.
5. **Build a real `ml_strategy.py` or downgrade `plan.md` Phase 4** — pick one. → plan 5 min; implementation 1-2 days.

**Skipped**: integrated e2e backtest, Rust `lib.rs` scaffolding, full live Binance test. Add when moving to those phases.

---

## Verbatim script evidence

```
ls src/cryptobot/
  → __init__.py allocator/ altdata/ api/ backtest/ cli/ config.py core/
    data/ exchanges/ execution/ funding/ market_data/ ml/ monitoring/
    risk/ strategies/ utils/ xmr/

ls src/cryptobot/strategies/
  → base.py funding_arb.py market_making.py mean_reversion.py
    registry.py stat_arb.py trend_following.py   (no ml_strategy.py)

ls src/cryptobot/ml/
  → __init__.py features.py models/ online.py
ls src/cryptobot/ml/models/
  → direction.py   (no volatility.py, regime.py, ensemble.py)

ls crates/cryptobot-core/src/
  → events/ math/ time/ types/   (all empty)
ls crates/cryptobot-{backtest,features,orderbook,py,risk,stats}/src/
  → (empty)

ls deploy/k8s/
  → 00-namespace.yaml 01-config.yaml 02-secret.yaml 03-pvc.yaml
    04-deployment.yaml kustomization.yaml   (no Service, no HPA)

ls monitoring/
  → alertmanager/ dashboards/ grafana/ prometheus/   (no loki/, no promtail/, no nginx/)

grep "print(" src/cryptobot/{core/clock.py,backtest/engine.py,strategies/base.py}
  → (no output)

grep "Counter\|Gauge" src/cryptobot/monitoring/metrics.py | grep -i "pnl"
  → total_pnl = Gauge(  (B025/B037 resolved)
```

---

**Update 2026-07-31 (post-audit fix)**: Scaffolded `monitoring/{loki,promtail,nginx}` directories with minimal configs. `docker compose config` (default profile) now passes.
