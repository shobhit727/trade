# Audit 2026-07-31 v2 (post-fix drift)

> **Confidence**: High (full directory walk + grep + cargo/rust inspection).
> **Scope**: re-validate `plan.md`, `PROJECT_MEMORY/`, audit `25_Audit_2026-07-31.md` against the repo after the B051-B069 + compose-scaffold + K8s-complete + ml_strategy + data/features + lightgbm-drop fixes landed via `04bfecf / 90efd8f / 0337acb / adc5334 / 97b13f6 / 519fc7f`.
> **Method**: directory walk of `src/`, `tests/`, `crates/`, `configs/`, `deploy/`, `monitoring/`, `docker/`, `scripts/`, `migrations/`, `docs/`, root. Grepped `Counter/Gauge` in `monitoring/metrics.py`, `_flatten_yaml`/`from_yaml_safe` in `config.py`, `lightgbm` in `requirements/`, `ml_strategy`/`data.features` in `src/`. `cargo build` not run (workspace non-buildable — see §1).

## TL;DR

Most items called out by `25_Audit_2026-07-31.md` are now **resolved**. The remaining drift is small:

- `25_Audit_2026-07-31.md` and several `PROJECT_MEMORY/0X_*.md` files (01, 02, 04, 05, 06, 07, 09, 11, 15, 17, 19, 20, 21, 22) still snapshot the **pre-fix** state.
- `13_Bug_Tracker.md` has duplicate `B026/B042/B043/B053` rows (one Open, one Resolved — the older Open row was not removed).
- Rust workspace: 6 of 7 member crates have no `Cargo.toml` despite being listed in workspace `members`. `cargo build` from root fails on missing member manifests.
- 6 dead empty dirs still under `src/cryptobot/`.

## 1. Diffs vs `25_Audit_2026-07-31.md`

| # | v1 audit claim | Reality now | Action |
|---|---|---|---|
| 1 | "ml_strategy.py does not exist" | `src/cryptobot/strategies/ml_strategy.py` 145 lines, `MLStrategy` + `MLStrategyConfig`; registered via `_STRATEGY_REGISTRY_MAP` | Close B054 |
| 2 | "data/features.py missing" | `src/cryptobot/data/features.py` 28 lines, re-exports `cryptobot.ml.features` | Close B056 |
| 3 | "K8s missing Service, HPA" | `deploy/k8s/05-service.yaml` (ClusterIP), `06-hpa.yaml` (v2 HPA CPU/memory); referenced in `kustomization.yaml` | Close B053 |
| 4 | "lightgbm in prod.txt, unused" | `lightgbm` removed from `requirements/prod.txt` (per `90efd8f` B055) | Close B055 |
| 5 | "configs/base.yaml ml.models.direction: lightgbm" | Now `sklearn_logreg`; `volatility` / `regime` disabled (`enabled: false`) | Close B058 |
| 6 | "strategies.enabled not read" | `registry.load_strategies_from_config` reads it; `_STRATEGY_REGISTRY_MAP` covers 6 names | Close B057/B059 |
| 7 | "data/ingestion no session reuse" | `_ensure_session()` with lock + per-call reuse | Close B042 |
| 8 | "monitoring/health.py no runtime updates" | `unregister_check`, `update_check_interval`, `get_check` added | Close B043 |
| 9 | "compose default profile broken" | `monitoring/{loki,promtail,nginx}` scaffolded with `local-config.yaml`, `config.yml`, `nginx.conf` + `conf.d/` | Close compose defect |
| 10 | "Health check exchange.symbols empty" | Fallback to `[settings.exchange.default_symbol]` (B066) | Close B066 |
| 11 | "sqlite DB path lost in container" | `/app/data/cryptobot.db` mounted (B069) | Close B069 |
| 12 | "Backtest equity double-counts unrealized PnL" | Manual equity update removed (B063); zero entry-price guarded (B064) | Close B063/B064 |
| 13 | "ML walk-forward normalization leak" | Train stats stored in `_feature_means/_stds`, used at predict (B065) | Close B065 |

## 2. Remaining drift in `PROJECT_MEMORY/`

Doc files still snapshotting pre-fix state (most say "Last Updated 2026-07-29"):

| Doc | What it still says that's wrong |
|---|---|
| `02_Module_Index.md` | Tree missing `ml/`, `data/features.py`, 4 extra strategies, `execution/{router,adverse_selection}`, `execution/venue/binance.py`, `utils/health_server.py`. Says "1 test file"; actually 22. Reports stale LOC. |
| `04_Data_Flow.md` | "cryptobot.ml pipeline: empty", "execution.venue.binance: not implemented", "data/backtest/data.py: missing", "backtest/reporting.py: missing" — all wrong. |
| `05_Control_Flow.md` | "Today the CLI is a placeholder", "RiskCheckResult` line shows old `Decimal("0")` fallback (B060 fixed)". |
| `06_API_Reference.md` | Strategies list stops at 3 entries; missing all 6 concrete strategies. `cli.main`: "Subcommands: backtest, validate, paper. All currently placeholder." |
| `07_Database_Model.md` | "No SQL migration files in `migrations/`" — both `001_extension.sql`, `002_hypertables.sql` exist. |
| `09_External_Dependencies.md` | Mentions `monero-rpc` removal but missing from prod.txt (good); still says "Cargo.toml: no crate members" (now 7 declared; only 1 has manifest). |
| `11_Testing.md` | "1 test file" / "test_core_foundation.py only" — actual 22. |
| `15_Design_Patterns.md` | Anti-pattern list still mentions print/Prometheus Counter/YAML swallow — all resolved. |
| `17_Performance.md` | Hot path list still mentions `execution/engine.py` `Decimal("0")` fallback (B040/B061 fixed). |
| `19_Open_Questions.md` | "Does `cryptobot` service's `/health` actually exist" — `utils/health_server.py` answered. "execution/venue/binance.py" listed as missing — now ✅. |
| `20_Assumptions.md` | AV-5: "BinanceWSClient.start works with empty symbols (likely empty URL)" — B044 fixed. |
| `21_Risk_Assessment.md` | R5/R10/R11/R12/R8 list as risks — all Resolved. |
| `22_Improvement_Ideas.md` | "Add `cryptobot.execution.venue.binance`", "Wire `cryptobot.cli.main` backtest", "Add `cryptobot.ml.features`" — all done. |

## 3. Bug tracker drift (`13_Bug_Tracker.md`)

- Duplicate rows for **B026**, **B042**, **B043**, **B053** — the original "Open" row was not removed when the "Resolved" row was added in the lower section.
- **B035** appears once (Resolved). OK.
- **B025/B037** still listed once each as Resolved in the upper table. OK.
- "Will-surprise areas" section still describes B038-style behavior that is now fixed.

## 4. Remaining real defects (verified)

### High

1. ~~**Rust workspace non-buildable.**~~ **Resolved 2026-07-31**: `Cargo.toml [workspace] members` trimmed to `["crates/cryptobot-core"]`; the 6 empty sibling dirs deleted; `crates/cryptobot-core/src/lib.rs` stub + 1 unit test added; `[target.*] rustflags` moved to `.cargo/config.toml` (illegal in virtual manifest); `[build-profile.*]` legacy key fixed to `[profile.*]`; dead `python = ["pyo3"]` feature dropped (referenced missing `pyo3` dep). `cargo build` + `cargo test` verified clean with rustup stable 1.97.1.

### Medium

2. **`crates/cryptobot-core/src/` has empty `{events,math,time,types}/` subdirs** and no `lib.rs`. Cargo manifest declares the package but `cargo build -p cryptobot-core` would fail.

3. **Dead empty dirs**: `src/cryptobot/{allocator,altdata,api,exchanges,funding,xmr}/`. No code references. Plan to keep is unclear.

4. **`monitoring/metrics.py` + `monitoring/alerting.py` module-level optional deps** — **B051 resolved 2026-07-31** in audit v4: `metrics.py` exposes `PROMETHEUS_AVAILABLE` + `_NoOpMetric` stubs; `alerting.py` defers `import aiohttp` to per-channel `_send`; `tests/unit/test_monitoring_lazy_imports.py` exercises the no-op fallback via subprocess stub-injection.

### Low

5. **`pytest.ini` + `pyproject.toml [tool.pytest]`** both define `asyncio_mode = auto`. Mild drift if one changes.

6. **`dashes` in plan.md section 3** — stray `)` after a node label. Cosmetic.

## 5. plan.md vs reality

| plan claim | Verified |
|---|---|
| Phase 4: `[x] ML-driven strategy` | ✅ |
| Phase 6: `[x] direction classifier, walk-forward training, drift detection` | ✅ |
| Phase 8: `[x] K8s manifests` + `Section 2 ⚠️ No Service, no HPA` | ⚠️ self-contradictory in same file. Service/HPA now exist. |
| Phase 2: WFA/MC `Section 2 table` says "✅" | OK |
| Section 5b: "All 7 crates exist with Cargo.toml + empty src/" | ⚠️ Only 1 of 7 has Cargo.toml |
| Section 12 blockers: "Rust workspace non-functional — cargo build fails", "ML volatility/regime/ensemble not implemented" | ✅ correct on first; ✅ correct on second |
| Section 12 blockers: "(Phase 4/6/8 overlap): ... ml_strategy.py created" | ✅ |
| Section 9 step ordering assumes no implementation; all steps complete | can be marked done |

## 6. Plan ↔ Memory ↔ Code alignment (post-fix)

| Topic | plan.md | PROJECT_MEMORY | code |
|---|---|---|---|
| Event bus / Clock / Portfolio | ✅ | ✅ | ✅ |
| ML features | ✅ | ✅ | ✅ |
| ML direction classifier + walk-forward + drift | ✅ | ✅ | ✅ |
| ML volatility / regime / ensemble | 🔲 | 🔲 | 🔲 |
| `ml_strategy.py` | ✅ | mixed (12 ⚠️, 13 unresolved B054, 00 ✅) | ✅ |
| `data/features.py` | ⚠️ says "use ml/features.py" | mixed (12 ⚠️ "Missing", 00 ✅) | ✅ (re-export) |
| K8s Service / HPA | self-contradicts ✅/⚠️ | ✅ | ✅ |
| `pyproject.toml` | ✅ | ✅ | ✅ |
| Migrations SQL | ✅ | ✅ | ✅ |
| Rust crates | "manifest + empty src/" — inaccurate | "manifest + empty src/" | 6/7 missing manifest too |
| Docker compose default profile | ✅ | ✅ | ✅ |
| Risk notional on no-price | not mentioned | ✅ | ✅ |
| ML strategy real impl | ✅ | ✅ (after B054 close) | ✅ |
| Strategies registry wired to YAML | not mentioned | mixed | ✅ via `load_strategies_from_config` |

## 7. Top fixes (new) — status

1. **Fix Rust workspace** — ✅ resolved 2026-07-31 (trimmed `members` array, deleted 6 empty sibling dirs, added `lib.rs` stub + unit test, fixed illegal `[target.*]` in virtual manifest, fixed `[build-profile.*]` legacy key, dropped dead `python` feature referencing missing `pyo3`). `cargo build` + `cargo test` clean.
2. **Reconcile bug tracker** — ✅ done in audit v2.
3. **Refresh stale `PROJECT_MEMORY/0X_*.md`** — ✅ done in audit v2 pass (01, 02, 04, 05, 06, 07, 08, 09, 10, 11, 12, 13, 14, 15, 17, 19, 20, 21, 22, 25).
4. **Clean up `plan.md` Section 8 self-contradiction** — ✅ done in audit v2.
5. **Decide on 6 dead empty dirs** under `src/cryptobot/` — `cryptobot-` Rust crates done; `src/cryptobot/{allocator,altdata,api,exchanges,funding,xmr}/` still pending.
6. **Mark `12_Feature_Status.md` rows** — ✅ done in audit v2.
7. **Close out B054/B056/B053/B057/B058/B059** — ✅ done in audit v2.

**Skipped**: live Binance e2e, full ML pipeline, Rust core engine, integration tests.
